// Temporary placeholder entry point so the `orchard-cli` executable target
// has a source file. MUST stay a top-level-code file (named main.swift) —
// an @main type cannot coexist with it in the same target. Replaced by
// `OrchardCLI.swift` (@main, ArgumentParser) in the next task.
print("orchard-cli placeholder")
